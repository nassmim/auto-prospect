# MAILER
RESEND_API_KEY=""

# WHATSAPP (Baileys)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
WHATSAPP_ENCRYPTION_KEY=""

# SMS API (for encrypting user's SMS API keys)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SMS_API_KEY_ENCRYPTION_KEY=""

# WORKER API
WORKER_API_URL="http://localhost:3001"
WORKER_API_SECRET="your-secret-here"